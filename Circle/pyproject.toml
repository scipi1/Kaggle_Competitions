[tool.poetry]
name = "circle"
version = "0.1.0"
description = ""
authors = ["Francesco Scipione <francesco.scipi1@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
kivy = "^2.2.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
